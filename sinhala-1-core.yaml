languages: [Sinhala]

description: The "Mooniak Sinhala Core" TODO

namespace: sinhala # For example: name-sinhala

categories:
  Punctuation:
    kunddaliya:

  Letters:
    vocalicR:
    vocalicRr:
    vocalicL:
    vocalicLl:
    nyja:
    nga:

  signs:
    repha: #(repaya)

  Typographical ligatures:
    # See attested/requierd shaping list generated from ligatrue data in 0-kernal
    # virama ligatrues: drop the trailing "a" 
    # signI ligatures : drop the trailing "a", suffix "I"
    # signIi ligatures : drop the trailing "a", suffix "Ii"
    # signU ligatures : drop the trailing "a", suffix "U"
    # signUu ligatures : drop the trailing "a", suffix "Uu"
    # rakar ligatures : drop the trailing "a", suffix "Ra"
    # rakar + virama ligatures : drop the trailing "a", suffix "R"
    # rakar + signI ligatures : drop the trailing "a", suffix "RI"
    # rakar + signIi ligatures : drop the trailing "a", suffix "RIi"

    signAa_virama: # (diga aela-pilla) # Included for better shaping.

  Ligated consonant conjuncts:
    # Ligated consonant conjuncts (සංයෝග අකුරු Sanyoga Akuru)
    kSsa:
